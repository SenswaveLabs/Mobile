import { StyleSheet } from "react-native";

export const keyboardOffset: number = 10;

export const defaultStyles = StyleSheet.create({
    defaultHorizontalPadding: {
        paddingHorizontal: 15,
    },
});
